// While
let index = 1;
while(index < 5) {
    
    // index++
    // ++index
    console.log(`entro ${index} vez`);
    index = index + 1 ;
}

// Do While
// FOR

for(let i = 0 ; i <= 5 ; i++) {
    //code
}

//in of
const numbers = [8, 9, 6, 88];

// for(let num in numbers) {
//     console.log(num);    
// }

for(let num of numbers) {
    console.log(num);    
}

